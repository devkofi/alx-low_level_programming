#include <stdio.h>

/**
 * main - Entry to the main program
 *
 * Return: Returns success (0)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
